import ApplicationContainer from '@elements/autonomous/containers/application-container';

beforeAll(async () => {
  await ApplicationContainer.define();
});

it('should create map NBT file with dimension as a short', () => {

});